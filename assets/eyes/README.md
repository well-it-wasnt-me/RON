# Eye assets

Place 1.28" round TFT sprite sheets here. The renderer in
`robot.eye_engine.renderer` is a pure-software fallback; once a
hardware-accelerated path is implemented, drop the pre-rendered frames in
this directory.
