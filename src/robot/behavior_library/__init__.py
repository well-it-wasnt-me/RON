"""Reusable behaviors.

A behavior is a short, named routine that drives both the face and the
body. The library ships a few ready-made ones (Greeting, Thinking,
Listening, Sleeping, Excited) and provides the building blocks
(:class:`BehaviorStep`) so the user can compose their own.
"""
